\documentclass{article}
\usepackage{blindtext}
\usepackage{geometry}
\usepackage{graphicx}
\graphicspath{{./figs/}}
\usepackage{multirow, multicol, longtable}
\usepackage{url}
\usepackage[colorlinks=true, linkcolor=blue, citecolor=blue, urlcolor=blue]{hyperref}
\geometry{
	a4paper,
	total={170mm,257mm},
	left=20mm,
	top=20mm,
}
\usepackage[authoryear]{natbib} 
\usepackage{longtable}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{algpseudocode}
\usepackage{algorithm}
\usepackage{anyfontsize}
\usepackage{lineno}
\usepackage{authblk}
\linenumbers
\usepackage{setspace}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{amsmath, amsfonts, booktabs, textcomp}
\usepackage{siunitx, microtype}

\title{
	A High-Performance Rule-Based Multi-Agent Framework for Spatio-Temporal 
	Vector Dynamics: Application to \textit{Anopheles stephensi} in Ethiopia}

\author[1,2]{Salvador D. Atagong}
\author[1]{Komi Mensah Agboka}
\author[1]{Kennedy Senagi}
\author[2,3]{Henri Tonnang}
\author[2]{John Odindi}

\affil[1]{International Center of Insect Physiology and Ecology (ICIPE)}
\affil[2]{University of KwaZulu-Natal, South Africa}
\affil[3]{International Institute of Tropical Agriculture (IITA), Ibadan, Nigeria}
\date{}

\begin{document}
\maketitle

\begin{abstract}


\paragraph{Keywords: multi-agent systems, vector-borne disease, \textit{Anopheles stephensi}, Metzler matrix, 
high-performance computing, spatial ecology.} 
\end{abstract}

\section{Introduction}
\label{sec:introduction}
\par
Vector-borne diseases (VBDs) represent one of the most profound challenges to global health security, accounting for 
approximately 700,000 deaths annually and imposing significant economic burdens on developing nations \citep{2025.RL.HMD}. 
Malaria alone threatens nearly half the world's population, with Sub-Saharan Africa bearing 94\% of the global case burden 
as \citep{2024.WH.WWM}. While the past two decades witnessed substantial reductions in malaria mortality through coordinated 
interventions, this hard-won progress faces unprecedented threats from ecological and demographic shifts. 
The rapid urbanization of Sub-Saharan Africa, currently the world's fastest urbanizing region \citep{2019.IS.UAI}, 
has created novel transmission ecologies that challenge historical assumptions about vector ecology and control 
strategies \citep{2024.NK.MBD}.
\par
The primary driver of this epidemiological transition is the globalization of previously localized vectors, particularly 
the invasive malaria mosquito \textit{Anopheles stephensi}. Unlike endemic African malaria vectors that predominantly 
breed in natural water bodies, \textit{An. stephensi} exhibits remarkable ecological plasticity, thriving in artificial 
containers and man-made structures characteristic of urban environments \citep{2025.GC.SAB}. This species shares critical 
adaptive traits with \textit{Aedes aegypti}, the primary dengue vector, including container breeding, anthropophily, and 
exophilic resting behaviors \citep{2022.AH.TPI}. The consequences of this adaptation were discored in Djibouti, 
where\textit{ An. stephensi} establishment triggered a 30-fold increase in malaria cases within seven years, transforming a 
nation on the verge of malaria elimination into a high-transmission zone \citep{2022.AH.TPI}. This vector's capacity to exploit 
previously low-risk urban settings necessitates fundamentally new approaches to surveillance, modeling, and 
intervention design \citep{2024.TM.AQA}.
\par
A critical bottleneck in responding to this emerging threat is the pervasive "Data Gap" characterizing many resource-constrained 
settings where \textit{An. stephensi} is establishing. Effective vector control relies on robust decision-support systems built 
upon high-quality, high-resolution entomological and epidemiological data, yet national surveillance programs often operate with 
limited financial and technological resources \citep{2025.RL.HMD}. Current modeling approaches struggle to bridge the gap between 
theoretical elegance and practical utility: empirical models propagate measurement errors through unreliable data streams, while 
process-based models frequently lack the spatial granularity and computational efficiency needed for operational 
deployment \citep{2025.RL.HMD}. This disconnect is particularly acute for invasive vectors, where absence of historical 
baseline data undermines traditional statistical approaches \citep{2019.EL.ALO}.
\par
To address these challenges, we propose a novel computational framework that integrates high-performance computing methodologies 
with ecological process modeling. Our system implements a layered, rule-based Multi-Agent Simulation (MAS) architecture that 
harmonizes heterogeneous spatio-temporal data streams into a unified analytical environment. This framework is part of the 
family of ABS (Agent Based Simulation) which are simulation models where the researcher explicitly describe the decision 
processes of simulated actors at micro-level \citep{2008.PE.ITM}. The proposed framework bridges Computer Science and 
Environmental Science by conceptualizing environmental data as dynamic information layers, including static rasters (for instance: 
elevation, building density, population density), time-series climate variables, and vector data (shapefiles), that interact with 
autonomous agent populations. We distinguish between two agent classes: "Inert Agents" representing static environmental features 
(urban water containers serving as breeding sites) and "Living Agents" modeling biological vectors (An. stephensi mosquitoes for 
example) with behavioral rules derived from empirical studies.
\par
This study demonstrates how such synthetic modeling approaches can provide critical insights where field data are scarce or 
unreliable. Using Dire Dawa, and Somali woredas from Ethiopia, which are regions experiencing \textit{An. stephensi} invasion, 
\citep{2024.GZ.ROC,2023.DH.FRO} as a proof-of-concept, we simulate 6 months of mosquito population dynamics across the whole regions. 
Our results illustrate how agent-based modeling can identify high-risk micro-habitats, simulate seasonal population trajectories, and 
potentially assist intervention strategies in data-limited settings. By making the simulation framework open-source and computationally 
efficient, we aim to empower public health agencies in resource-constrained regions with decision-support tools capable of addressing the 
unique challenges posed by urban-adapted invasive vectors.


\section{Materials and Methods}
\label{sec:methods}
\subsection{Framework Architecture}
\label{sec:framework}
\subsubsection{Overview}
% comprehensive chart of the framework
The simulation framework follows a modular, agent-based design that integrates spatially explicit environmental data, 
temperature-dependent life-cycle models, and behavioural rules. It is implemented in Java and built around four core 
subsystems: (i) a time manager that advances simulation time in discrete ticks (15-minutes per tick) and interpolates 
climate data; (ii) a spatial registry that indexes agents on a lazy grid for efficient neighbourhood queries; 
(iii) an agent layer system that manages agents and evaluates rules in parallel; and (iv) a lifecycle model that computes 
temperature-driven development and survival rates. The overall architecture is illustrated in Figure~\ref{fig:arch}.
\begin{figure}[!htb]
\centering
\includegraphics[width=\textwidth]{figs/phd_graphs-rmas.drawio.png}
\caption{High-level architecture of the simulation framework. The diagram is organised into three layers: 
\textbf{Data Layer} (input data sources), \textbf{Core Engine Layer} (simulation components), and \textbf{Analysis 
\& Visualization Layer} (post-processing and validation). Solid arrows ($\rightarrow$) denote \textit{data flow} 
(e.g., configuration files, rasters, climate NetCDF, and simulation outputs). Dashed arrows ($\rightarrow$) denote 
\textit{control flow} (e.g., tick scheduling, spatial queries, rule evaluation, and lifecycle updates). The analysis 
layer reads CSV snapshots generated by the persistence service and produces daily summaries, suitability maps, 
validation metrics, and final figures.}
\label{fig:arch}
\end{figure}

\subsubsection{Core Components}
% one fleshy paragraph per components (agent layers, raster layers, lifecycle models, etc.)
\paragraph{Agent layers.} Two agent types are implemented: \texttt{LivingAgent} (representing a mosquitoe) 
and \texttt{InertAgent} (representing a breeding site). Living agents track life stage (egg, larva, pupa, adult), age, 
energy, gravid status, and resting behaviour. Their movement is modelled as a random walk with a step size derived from field observations 
of flight range \citep{1966.MA.FRL}. Inert agents maintain water volume, egg count, larval count, and carrying capacity; they update their 
capacity dynamically based on water level, mimicking the density-dependent mortality observed in larval habitats \citep{2016.ST.OTI}.  
Agent layers are processed in parallel using a partition-based container (\texttt{AgentContainer}) and a rule-engine that evaluates 
behavioural rules (e.g., feeding, oviposition, resting) in priority order \citep{2023.ST.ASR}.

\paragraph{Raster layers and climate data.}  
Static raster layers (elevation, building density, population density) are loaded from GeoTIFF files using GeoTools and stored either 
in memory or memory-mapped for large grids \citep{2021.MB.AUO}. Climate time series (2-m temperature and total precipitation) are ingested 
from NetCDF files (ERA5-Land reanalysis) into interpolated raster layers that perform linear temporal interpolation between hourly 
steps \citep{2014.GC.AED, 2017.CL.IMS}.

\paragraph{Lifecycle model.}\label{sec:lifecycle} 
The lifecycle model represents mosquito population dynamics using a discrete-time, stage-structured Metzler matrix 
\citep{2023.ST.ASR, 2025.KM.WTR}. Four developmental stages are tracked: eggs ($E$), larvae ($L$), pupae ($P$), and adults ($A$). 
All rates are temperature-dependent and defined on a per-day basis, then converted to tick-level probabilities via the Poisson 
process ($p = 1 - \exp(-r \Delta t)$, where $\Delta t$ is the tick duration in days). The daily projection matrix is:

\[
\mathbf{M}(T) = 
\begin{pmatrix}
(1 - d_E) S_E & 0 & 0 & F \cdot K \\
d_E S_E & (1 - d_L) S_L & 0 & 0 \\
0 & d_L S_L & (1 - d_P) S_P & 0 \\
0 & 0 & d_P S_P & 1 - \mu_A
\end{pmatrix}
\]

with stage abundances evolving as $[E,L,P,A]_{t+1}^\top = \mathbf{M}(T) [E,L,P,A]_t^\top$. The temperature-dependent functions are:

\begin{itemize}
  \item \textbf{Development rates} (day$^{-1}$): $d_X(T) = \max(0, a_X T^2 + b_X T + c_X)$ for $X \in \{E,L,P\}$.
  \item \textbf{Stage survival} (Gaussian): $S_X(T) = \text{amp}_X \cdot \exp\left(-\frac{1}{2}\left(\frac{T - \mu_X}{\sigma_X}\right)^2\right)$.
  \item \textbf{Adult mortality} (day$^{-1}$): $\mu_A(T) = \max(0, a_A T^2 + b_A T + c_A)$.
  \item \textbf{Fecundity} (eggs/female/day): $F(T) = \max\left(0, a_F e^{b_F T} - e^{b_F T_{\max} - \left(\frac{T_{\max}-T}{c_F}\right)^2}\right)$.
  \item \textbf{Host carrying capacity}: $K = 1 + \ln(1 + \text{population density})$, which scales fecundity based on human population density.
  \item \textbf{Density-dependent larval mortality}: each aquatic habitat has a carrying capacity $\text{cap}$ (function of water volume); 
  if larval count exceeds $\text{cap}$, excess larvae are removed probabilistically.
\end{itemize}

All temperature-response coefficients are stored in a YAML configuration file under the \texttt{species} key, making the model generic and easily 
adaptable to any mosquito species by replacing the coefficients in Table~\ref{tab:lifecycle_params}. This parametrisation 
approach follows the principles of generic insect population modelling \citep{2025.KM.DPF, 2025.KM.WTR, 2026.KM.THM}.

\begin{table}[htbp]
\centering
\caption{Generic temperature-response parameters for a mosquito lifecycle model.}
\label{tab:lifecycle_params}
\begin{tabular}{llll}
\toprule
Parameter set & Meaning & Unit & YAML key \\ \midrule
$d_E$ coeff. & Egg development rate & $1/^\circ\text{C}^2, 1/^\circ\text{C}, 1$ & \texttt{egg\_dev\_a,b,c} \\
$d_L$ coeff. & Larva development rate & $1/^\circ\text{C}^2, 1/^\circ\text{C}, 1$ & \texttt{larva\_dev\_a,b,c} \\
$d_P$ coeff. & Pupa development rate & $1/^\circ\text{C}^2, 1/^\circ\text{C}, 1$ & \texttt{pupa\_dev\_a,b,c} \\
$S_E$ & Egg survival (amp, mean, sigma) & dim., $^\circ\text{C}, ^\circ\text{C}$ & \texttt{egg\_survival\_...} \\
$S_L$ & Larva survival (amp, mean, sigma) & dim., $^\circ\text{C}, ^\circ\text{C}$ & \texttt{larva\_survival\_...} \\
$S_P$ & Pupa survival (amp, mean, sigma) & dim., $^\circ\text{C}, ^\circ\text{C}$ & \texttt{pupa\_survival\_...} \\
$\mu_A$ coeff. & Adult mortality & $1/^\circ\text{C}^2, 1/^\circ\text{C}, 1$ & \texttt{adult\_mort\_a,b,c} \\
$F$ coeff. & Fecundity & $a_F, b_F, T_{\max}, c_F$ & \texttt{fecundity\_a,b,Tmax,c} \\
$K$ & Host scaling factor & dimensionless & (derived) \\ \bottomrule
\end{tabular}
\end{table}


\paragraph{Rule engine and spatial registry.}  
Behavioural rules (e.g., “\texttt{feed}”, “\texttt{lay\_eggs}”, “\texttt{rest\_in\_building}”, etc.) are defined in a YAML 
configuration file and evaluated using a JavaScript engine (GraalVM). The spatial registry implements a lazy grid with a 
configurable cell size (default $0.001/^\circ\text{C} \approx 110 m$). Neighbour queries are performed by checking cells 
within a Chebyshev distance $\lceil \text{radius}/\text{cellSize}\rceil$ and cache results for one second to reduce 
computational cost.


\subsubsection{Simulation Configuration}
% how to configure a simulation
\par
All simulation parameters are defined in a single YAML file (\texttt{simulation.yaml}). The file specifies:  
\par
\begin{itemize}
  \item \textbf{Time settings}: start date, total number of ticks, tick duration (15-minutes).  
  \item \textbf{File paths}: study area shapefile, elevation, building density, population rasters, and the climate NetCDF.  
  \item \textbf{Species parameters}: coefficients for development, survival, fecundity, and adult mortality.  
  \item \textbf{Tokens}: mapping between environment variable names (e.g., “temperature”) and raster layers.  
  \item \textbf{Agent layers and rules}: each layer contains a list of rules with a condition, an action, and a priority.  
  \item \textbf{Seeding}: number of breeding sites and mosquitoes, habitat grid resolution, and whether to seed uniformly or 
  using a weighted habitat suitability map.  
  \item \textbf{Project defaults}: agent search radius, step size, maximum age.
\end{itemize}
\par
At startup, the \texttt{App} class loads the configuration, initialises the time manager, loads rasters and climate data, 
creates the spatial registry, builds agent layers, seeds the initial population, and starts the simulation engine.

\subsubsection{Result Analysis}
\label{sec:analysis}
% details on the analysis notebook how are simulation result analyzed details
Simulation outputs are exported as CSV snapshots every 100 ticks (25 days). Each snapshot contains per-agent records 
with tick count, agent ID, layer, type, coordinates, alive flag, age, stage, energy, gravid status, and for inert agents, egg/larval 
counts and water volume. Environmental values (temperature, precipitation, etc) are attached to each agent's row via bilinear interpolation 
from the current climate frame.
\par
The analysis notebook (Python) performs the following steps:
\par
\begin{enumerate}
  \item \textbf{Daily aggregation}: The CSV is read in chunks (100 000 rows) to compute daily adult, larva, and pupa counts, as well 
  as tank statistics (mean water volume, total eggs/larvae), using the tick-to-day conversion ($96$ ticks per day).  
  \item \textbf{Population dynamics}: Stage counts are plotted, and stage durations are estimated from cross-correlation of smoothed 
  population peaks. Theoretical durations are computed from species parameters at the mean observed temperature.  
  \item \textbf{Environmental correlation}: Pearson correlation between daily adult counts and mean temperature / precipitation is calculated.  
  \item \textbf{Suitability mapping}: A kernel density estimate (KDE) of adult points (bandwidth 0.002° ≈ 220 m) is generated and exported 
  as a GeoTIFF. A breeding site potential raster is created by rasterising the sum of eggs and larvae in water tanks on the last tick.  
  \item \textbf{Validation}: The continuous Boyce index \citep{2006.HA.ETA} and the proportion of occurrence points falling into the top 20\% 
  of the suitability surface are computed using independently collected occurrence data (from GBIF, iNaturalist, Literature, clipped to 
  the study area).  
  \item \textbf{Export}: Daily summaries and a sample of raw data are saved for further analysis.
\end{enumerate}
\par
All figures and rasters are stored in a user-defined output directory.

\subsection{Case Study Design}
\label{sec:case_study}
\subsubsection{Vector and Study Site}
% Dire Dawa, Oromia Woredas Ethiopia , all the details and citations on why this study sites were selected
\par
The case study is set in Ethiopia, focusing on two contrasting regions: the city of Dire Dawa and selected \textit{woredas} (districts) 
in the Oromia region. Both areas have experienced recent incursions of the Asian malaria vector \textit{Anopheles stephensi} and are 
considered high-risk zones for urban and peri-urban malaria transmission \citep{2020.MB.GDO, 2021.MB.AUO, 2023.DH.FRO}.
\par
Dire Dawa (\ang{41.85} E, \ang{9.60} N) is a low-altitude (\SI{\sim 1200}{\meter}) urban centre with a semi-arid climate and a known 
history of \textit{An.~stephensi} establishment, first reported in 2018 \citep{2020.MB.GDO}. The Oromia study area comprises several 
\textit{woredas} (e.g., Adama, Bishoftu, and surrounding districts) that span an altitudinal gradient from \SIrange{1500}{2500}{\meter}, 
representing a mix of agricultural, peri-urban, and highland ecosystems where the vector has been detected since 
2019 \citep{2021.MB.AUO, 2024.TA.SDA}. These sites were selected because they provide a natural experiment to evaluate the model's 
ability to capture spatial heterogeneity in vector abundance driven by temperature, urbanisation, and host availability 
\citep{2025.TD.BOA, 2024.GZ.ROC}.

\subsubsection{Vector Biology}
% details on anopheles stephensi biology and equations with detail oh how parameters where caculated
% for the final matrix as well as the citations
% Required packages: \usepackage{amsmath, booktabs, siunitx}

\textit{Anopheles stephensi} is a highly adaptive urban malaria vector native to South Asia and the Middle East, now established 
in the Horn of Africa \citep{2024.RT.IAS}. Its life cycle is tightly coupled to temperature, and its larvae exploit a wide range 
of artificial containers, especially overhead water tanks and construction pits \citep{2016.ST.OTI}. 

The temperature-dependent functions described in Section~\ref{sec:lifecycle} were parameterised for \textit{An.~stephensi} using 
laboratory and field data from the literature (Table~\ref{tab:stephensi_params}). Development rates ($d_E, d_L, d_P$) and adult 
mortality ($\mu_A$) were fitted to the observations of \citet{2023.JJ.BBA} and \citet{2025.CG.EOT}. Survival probabilities 
($S_E, S_L, S_P$) were derived from the meta-analysis of \citet{2022.OC.TIT}. Fecundity coefficients ($a_F, b_F, T_{\max}, c_F$) 
were taken from \citet{2023.JJ.BBA}, who estimated daily egg production under varying thermal regimes. 

The host carrying capacity $K$ uses human population density (obtained from the WorldPop raster) as a proxy for biting resources, 
assuming that \textit{An.~stephensi} is highly anthropophilic in urban settings \citep{2024.TA.SDA, 2022.AH.TPI}. Density-dependent 
larval mortality follows the tank capacity model of \citet{2016.ST.OTI}, where capacity is linearly scaled with water volume 
(maximum 500 larvae at \SI{100}{\percent} water volume).

The resulting daily projection matrix for \textit{An.~stephensi} at a reference temperature $T = \SI{25}{\celsius}$ is:
\[
\mathbf{M}(25) = \begin{pmatrix}
0.48 & 0 & 0 & 8.2 \\
0.52 & 0.76 & 0 & 0 \\
0 & 0.24 & 0.70 & 0 \\
0 & 0 & 0.30 & 0.91
\end{pmatrix}
\]
where the dominant eigenvalue $\lambda_{\max} = 1.03$, indicating slow population growth under optimal conditions, consistent with 
field observations in Ethiopia \citep{2021.MB.AUO, 2022.AH.TPI}.

\begin{table}[htbp]
\centering
\small % Slighting smaller font to ensure table fits page width
\caption{Species-specific parameters for \textit{Anopheles stephensi} used in the case study.}
\label{tab:stephensi_params}
\begin{tabular}{@{}llll@{}}
\toprule
Parameter & Meaning & Value / coefficients & Source \\ \midrule
$d_E$ & Egg development (day$^{-1}$) & $a=-0.00098, b=0.0502, c=-0.348$ & \citet{2022.OC.TIT} \\
$d_L$ & Larva development (day$^{-1}$) & $a=-0.00082, b=0.0425, c=-0.305$ & \citet{2023.JJ.BBA} \\
$d_P$ & Pupa development (day$^{-1}$) & $a=-0.00074, b=0.0398, c=-0.291$ & \citet{2022.OC.TIT} \\
$S_E$ & Egg survival & $\text{amp}=0.94, \mu=26.2, \sigma=3.5$ & \citet{2022.OC.TIT} \\
$S_L$ & Larva survival & $\text{amp}=0.88, \mu=26.8, \sigma=3.8$ & \citet{2022.OC.TIT} \\
$S_P$ & Pupa survival & $\text{amp}=0.91, \mu=26.5, \sigma=3.6$ & \citet{2022.OC.TIT} \\
$\mu_A$ & Adult mortality (day$^{-1}$) & $a=0.00012, b=-0.0018, c=0.055$ & \citet{2025.CG.EOT} \\
$F$ & Fecundity (eggs/female/day) & $a_F=-0.0099, b_F=0.567, T_{\max}=32.0, c_F=2.97$ & \citet{2023.JJ.BBA} \\
$K$ & Host scaling factor & $1 + \ln(1+\text{popdens})$ & WorldPop (2020) \\ \bottomrule
\end{tabular}
\end{table}


\subsubsection{Data Sources}
% Required packages: \usepackage{siunitx, microtype, booktabs}

\label{sec:data_sources}
The simulation integrates five categories of data:

\begin{itemize}
    \item \textbf{Static environmental rasters} -- Elevation (SRTM, \SI{30}{\meter}), building density (Global 
	Human Settlement Layer), and human population density (WorldPop, 2020) were resampled to a common \ang{0.001} 
	(\SI{\sim 110}{\meter}) grid over the study area.
    
    \item \textbf{Climate time series} -- Hourly \SI{2}{\meter} temperature and total precipitation for 2020 were 
	obtained from the ERA5-Land reanalysis (Copernicus Climate Change Service) and stored as NetCDF files.
    
    \item \textbf{Vector occurrence data} -- Presence points for \textit{An.~stephensi} in Ethiopia were compiled from 
	the Vector Atlas database \citep{2025.VE.VAP}, GBIF \citep{2011.AN.BIG}, and recent entomological surveys 
	\citep{2020.MB.GDO, 2021.MB.AUO, 2023.DH.FRO}. Only records with precise coordinates and collection dates 
	in 2018--2022 were retained.
    
    \item \textbf{Bionomic and life-history data} -- Parameter values (Table~\ref{tab:stephensi_params}) were extracted 
	from peer-reviewed literature \citep{2022.OC.TIT, 2023.JJ.BBA, 2025.CG.EOT} and supplemented with expert knowledge.
    
    \item \textbf{Hydrographic information} -- Locations of potential breeding sites (overhead tanks, wells, construction 
	pits) were derived from high-resolution satellite imagery and local surveys \citep{2016.ST.OTI, 2024.TA.SDA}.
\end{itemize}

All data were harmonised to the WGS84 geographic coordinate system (EPSG:4326) and stored in a structured file hierarchy.

\subsubsection{Simulation Assupmtions, Rules and Configuration}
\label{sec:sim_assumptions}
% the rules and simulation configurations

The simulation makes the following key assumptions:
\begin{enumerate}  
    \item \textbf{Temperature is the primary abiotic driver} -- All development, survival, and reproduction rates respond 
	only to ambient temperature; effects of relative humidity, rainfall, and other variables are omitted or implicitly 
	captured through breeding site water dynamics.  
    
    \item \textbf{Host availability scales with human population density} -- The effective fecundity $F \cdot K$ uses 
	population density as a proxy for blood-meal availability, assuming that females feed preferentially on humans in 
	urban settings \citep{2022.AH.TPI}.  
    
    \item \textbf{Aquatic habitats are static} -- Water tank positions and their maximum water volume are fixed at 
	initialisation; only evaporation and rainfall modify water level over time.  
    
    \item \textbf{Mosquito movement is localised} -- Adults disperse via a random walk with a fixed step size 
	(\ang{0.00005} $\approx$ \SI{5.5}{\meter} per tick) and a search radius of \ang{0.005} (\SI{\sim 550}{\meter}), 
	reflecting limited flight range \citep{1966.MA.FRL}.  
    
    \item \textbf{Resting and feeding follow simple rules} -- Adults rest indoors or outdoors based on building 
	density and temperature, and they feed when energy falls below a threshold, with success depending on 
	local population density.
\end{enumerate}

The behavioural rules (Table~\ref{tab:rules}) are encoded in the YAML configuration file (\texttt{simulation.yaml}) and 
evaluated in priority order at each tick. The complete configuration used for the case study is provided as supplementary 
material.

\begin{table}[htbp]
\centering
\caption{Behavioural rules for \textit{An. stephensi} adults.}
\label{tab:rules}
\begin{tabular}{@{}llc@{}}
\toprule
Condition & Action & Priority \\ \midrule
Stage = Adult \textbf{and} energy $<$ 0.6 \textbf{and} not resting & \texttt{feed} & 8 \\
Stage = Adult \textbf{and} energy $>$ 0.7 \textbf{and} not gravid & \texttt{get\_gravid} & 7 \\
Gravid = True \textbf{and} $T > \SI{15}{\celsius}$ & \texttt{lay\_eggs} & 6 \\
$T < \SI{10}{\celsius}$ \textbf{or} $T > \SI{40}{\celsius}$ & \texttt{die} & 9 \\
Precipitation $>$ \SI{0.008}{\meter} \textbf{and} build. dens. $>$ 0.1 \textbf{and} not resting & \texttt{rest\_in\_building} & 5 \\
Stage = Adult \textbf{and} not resting & \texttt{move\_random} & 3 \\
Otherwise & \texttt{rest} & 2 \\ \bottomrule
\end{tabular}
\end{table}

The simulation was run for \SI{30}{\day} (tick = \SI{15}{\minute}, \num{2880} ticks) with a burn-in of \SI{5}{\day} to allow 
transients to decay. Output snapshots were exported every 100 ticks and analysed using the Python notebook described in 
Section~\ref{sec:analysis}.

% Required packages: \usepackage{siunitx, graphicx, booktabs, amsmath}

\section{Results}
\label{sec:results}

The simulation was executed for a duration of six months (\SI{180}{\day}, comprising \num{17280} ticks of \SI{15}{\minute} each) 
over the contrasting study areas of Dire Dawa and selected Oromia \textit{woredas} in Ethiopia. The model utilised the 
temperature-dependent lifecycle parameters specifically fitted for \textit{Anopheles stephensi}. Output snapshots were processed 
to evaluate population stability, environmental response, and spatial suitability.

The mosquito population reached a near-steady state after approximately \SI{10}{\day} of simulation. Across both sites, adult 
abundance stabilised at approximately \num{85000} individuals, while larval and pupal counts fluctuated around \num{35000} and \num{21000}, 
respectively (Figure~\ref{fig:population_dynamics}). Analysis of the six-month time series revealed that the daily adult count maintained 
a weak negative correlation with temperature (Pearson's $r = -0.119$, $p = 0.12$) and negligible correlation with precipitation 
($r = 0.009$, $p = 0.91$). These results suggest that under the simulated conditions, intrinsic density-dependent regulation and the 
availability of stable aquatic habitats (water tanks) dominated over short-term climatic fluctuations.

\begin{figure}[htbp]
     \centering
     \begin{subfigure}[b]{0.49\textwidth}
         \centering
         \includegraphics[width=\textwidth]{figs/dire_dawa/mosquito_dynamics.png}
         \caption{Dire Dawa}
     \end{subfigure}
     \hfill
     \begin{subfigure}[b]{0.49\textwidth}
         \centering
         \includegraphics[width=\textwidth]{figs/oromia/mosquito_dynamics.png}
         \caption{Oromia}
     \end{subfigure}
     \caption{Daily counts of adult, larval, and pupal \textit{An. stephensi} over the six-month simulation. Population levels show
	  high stability following the initial 10-day burn-in period.}
     \label{fig:population_dynamics}
\end{figure}

Theoretical stage durations were calculated based on the mean observed temperature (\SI{19.9}{\celsius}), yielding \SI{5.6}{\day} 
for larvae and \SI{8.4}{\day} for pupae. The dominant eigenvalue ($\lambda_{\max}$) of the Metzler projection matrix, averaged over 
the full simulation period, was \num{1.003}. This value indicates a slowly growing population near equilibrium, which is consistent 
with the established status of \textit{An.~stephensi} in urban Ethiopia \citep{2021.MB.AUO}. Figure~\ref{fig:growth} illustrates the 
moderate agreement between the observed daily adult growth factor and the predicted $\lambda_{\max}$.

\begin{figure}[htbp]
     \centering
     \begin{subfigure}[b]{0.49\textwidth}
         \centering
         \includegraphics[width=\textwidth]{figs/dire_dawa/growth_validation.png}
         \caption{Dire Dawa}
     \end{subfigure}
     \hfill
     \begin{subfigure}[b]{0.49\textwidth}
         \centering
         \includegraphics[width=\textwidth]{figs/oromia/growth_validation.png}
         \caption{Oromia}
     \end{subfigure}
     \caption{Observed daily adult growth factor versus predicted $\lambda_{\max}$ (red dashed line). The average predicted 
	 growth factor ($\lambda_{\max} = 1.003$) aligns with the observed stability.}
     \label{fig:growth}
\end{figure}

A total of \num{1336} water tanks were simulated. Mean water volume remained highly stable at $\SI{85.3 \pm 0.02}{\percent}$, 
as evaporation losses were balanced by periodic precipitation events (Figure~\ref{fig:tank_stats}). The reciprocal trend 
observed between egg and larval counts per tank confirms active hatching and stage transition within these permanent aquatic 
habitats.

\begin{figure}[htbp]
     \centering
     \begin{subfigure}[b]{0.49\textwidth}
         \centering
         \includegraphics[width=\textwidth]{figs/dire_dawa/tank_stats.png}
         \caption{Dire Dawa}
     \end{subfigure}
     \hfill
     \begin{subfigure}[b]{0.49\textwidth}
         \centering
         \includegraphics[width=\textwidth]{figs/oromia/tank_stats.png}
         \caption{Oromia}
     \end{subfigure}
     \caption{Water tank statistics: (top) mean water volume; (middle) mean eggs per tank; (bottom) mean larvae per tank.}
     \label{fig:tank_stats}
\end{figure}

Daily average temperatures ranged between \SIrange{19.5}{20.1}{\celsius}, while precipitation remained relatively constant 
at \SI{\sim 1590}{\milli\meter\per\day} (Figure~\ref{fig:environment}). The low variability in these drivers over the 
six-month period explains the dominance of endogenous population processes over climatic forcing in this specific case study.

\begin{figure}[htbp]
     \centering
     \begin{subfigure}[b]{0.49\textwidth}
         \centering
         \includegraphics[width=\textwidth]{figs/dire_dawa/environment.png}
         \caption{Dire Dawa}
     \end{subfigure}
     \hfill
     \begin{subfigure}[b]{0.49\textwidth}
         \centering
         \includegraphics[width=\textwidth]{figs/oromia/environment.png}
         \caption{Oromia}
     \end{subfigure}
     \caption{Daily average environmental conditions (Temperature in red, Precipitation in blue) throughout the simulation.}
     \label{fig:environment}
\end{figure}

Spatial suitability maps were generated using Kernel Density Estimation (KDE) with a bandwidth of \ang{0.002} 
(\SI{\sim 220}{\meter}). As shown in Figure~\ref{fig:adult_suitability}, adult mosquito density was highest in urban 
cores and areas with significant building density. Validation against independent occurrence points yielded a 
continuous Boyce index of \num{0.633}, confirming the model's moderate predictive power for invasive vector establishment. 
The final breeding site potential, derived from the distribution of eggs and larvae in water tanks, is presented in 
Figure~\ref{fig:breeding_potential}.

\begin{figure}[htbp]
     \centering
     \begin{subfigure}[b]{0.49\textwidth}
         \centering
         \includegraphics[width=\textwidth]{figs/dire_dawa/adult_suitability.png}
         \caption{Dire Dawa}
     \end{subfigure}
     \hfill
     \begin{subfigure}[b]{0.49\textwidth}
         \centering
         \includegraphics[width=\textwidth]{figs/oromia/adult_suitability.png}
         \caption{Oromia}
     \end{subfigure}
     \caption{Adult mosquito suitability maps (KDE) overlaid with occurrence records (red dots). High density 
	 correlates with urban infrastructure.}
     \label{fig:adult_suitability}
\end{figure}

\begin{figure}[htbp]
     \centering
     \begin{subfigure}[b]{0.49\textwidth}
         \centering
         \includegraphics[width=\textwidth]{figs/dire_dawa/breeding_site_potential.png}
         \caption{Dire Dawa}
     \end{subfigure}
     \hfill
     \begin{subfigure}[b]{0.49\textwidth}
         \centering
         \includegraphics[width=\textwidth]{figs/oromia/breeding_site_potential.png}
         \caption{Oromia}
     \end{subfigure}
     \caption{Breeding site potential rasterized from final egg and larval counts in water tanks.}
     \label{fig:breeding_potential}
\end{figure}


% Required packages: \usepackage{siunitx, amsmath}

\section{Discussion}
\label{sec:discussion}
\par
The simulation results demonstrate that the agent-based model successfully reproduces a near-equilibrium population of 
\textit{Anopheles stephensi} in the two contrasting Ethiopian settings of Dire Dawa and the Oromia \textit{woredas}. 
The observed adult abundance (\SI{\sim 85000} individuals), larval and pupal counts, and the dominant eigenvalue 
($\lambda_{\max} = 1.003$) indicate a slowly growing but stable population. This is consistent with field reports 
suggesting that \textit{An.~stephensi} has become established in urban Ethiopia without triggering explosive outbreaks 
\citep{2021.MB.AUO, 2022.AH.TPI}. Furthermore, the theoretical stage durations (\SI{5.6}{\day} for larvae and \SI{8.4}{\day} 
for pupae at \SI{19.9}{\celsius}) fall within the ranges reported in laboratory studies \citep{2023.JJ.BBA}, supporting the 
validity of the temperature-dependent parameterisation.
\par
A key finding of this study is the weak correlation between adult abundance and short-term temperature or precipitation 
fluctuations. Under the relatively stable climatic conditions of the six-month simulation (temperature range 
\SIrange{19.5}{20.1}{\celsius}), endogenous density-dependent processes and the availability of permanent larval 
habitats (water tanks) appear to outweigh environmental forcing. This aligns with observations that \textit{An.~stephensi} 
thrives in human-made containers that buffer the population against rainfall variability \citep{2016.ST.OTI, 2024.TA.SDA}. 
However, the model may underestimate the influence of extreme weather events or seasonal drying, which were not prominent in 
the 2020 climate data utilized. Future work should incorporate inter-annual climate variability to test population resilience 
against prolonged drought periods.
\par
The water tank statistics revealed a stable mean water volume (\SI{85.3}{\percent}) and a reciprocal pattern between egg and 
larval counts, confirming that hatching and density-dependent mortality are correctly simulated within the agent-based framework. 
The tank capacity model, which caps larval numbers as a linear function of water volume, effectively prevented unbounded growth 
and mimicked the resource competition observed in field surveys \citep{2016.ST.OTI}. Nevertheless, the assumption of static tank 
positions and fixed maximum volumes may oversimplify real-world dynamics, where water usage, cleaning, and replenishment vary 
seasonally. Integrating a stochastic turnover or "aging" of breeding sites could improve the model's structural realism.
\par
The spatial suitability maps generated via KDE highlight urban cores and high-building-density areas as the most suitable 
habitats for both adults and larvae. This pattern corroborates the known urban adaptation of \textit{An.~stephensi} and its 
preference for artificial containers \citep{2024.RT.IAS, 2022.EJ.ASA}. The continuous Boyce index of \num{0.633} indicates 
moderate predictive performance, which is acceptable for a presence-only model of a recently invasive species where occurrence 
data are sparse. The relatively low proportion of independent points (\SI{14.3}{\percent}) falling into the top \SI{20}{\percent} 
of the suitability map is likely a consequence of the small sample size remaining after spatial clipping ($n=14$). Increasing the 
collection of geo-referenced occurrence records across different urban gradients would enable a more robust validation in 
future iterations.
\par
Several limitations should be acknowledged. First, the model currently omits other biophysical drivers such as relative 
humidity and wind speed, which may affect adult desiccation and dispersal behaviour \citep{2024.EA.IAC, 2025.CG.EOT}. Second, 
the movement rule (random walk with a fixed step size) does not yet capture complex host-seeking behaviour; incorporating a 
biased random walk toward resource gradients could increase spatial accuracy. Finally, while human population density is a 
strong proxy for biting resources in urban settings, \textit{An.~stephensi} is known to exhibit plastic feeding behaviour 
including zoophily \citep{2016.ST.OTI}. A multi-host formulation might improve fecundity estimates in peri-urban or mixed 
land-use areas.
\par
Despite these limitations, the model provides a valuable tool for exploring the invasion dynamics of \textit{An.~stephensi} 
and for designing targeted vector control interventions. The generic, YAML-driven parameterisation allows for rapid adaptation 
to other mosquito species or geographic regions. Future research should extend the simulation to multi-year runs to evaluate 
the long-term impact of larval source management, such as the systematic covering of water tanks or targeted larviciding, on 
population suppression in urban centres.


\section{Conclusion}
\label{sec:conclusion}


\section*{Acknowledgments}
\par
This research was supported by the International Center of Insect Physiology and Ecology (ICIPE), 
the University of KwaZulu-Natal, and the International Institute of Tropical Agriculture (IITA). 
We acknowledge the providers of the open data used in this study, including GBIF, the Copernicus 
Climate Change Service (ERA5-Land), the Malaria Atlas Project, and the Vector Atlas consortium. 
We particularly thank the data contributors and curators who make their work publicly available, 
enabling research of this nature.

\section*{Code availability}

\bibliographystyle{apalike}
\bibliography{bibliography.bib}

\end{document}